/** Shared watchlist column grid — header + rows must use the same template. */
export const WATCHLIST_GRID =
  "grid w-full grid-cols-[minmax(0,1.2fr)_auto_minmax(0,1fr)_auto_minmax(0,1fr)] items-center"

export const WATCHLIST_DIVIDER =
  "select-none self-center px-1 text-[11px] font-light text-border/70"

export const WATCHLIST_COL =
  "flex min-w-0 flex-col items-center justify-center text-center"
